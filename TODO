
- move all storage stuff to a separate menu
	main -> storage -> hd/ctl/attach

- tidy up
	- move the repeated tmpfile stuff into a single funcion
	- new function for on/off settings

- modifyvm-net
	- nat settings
	- can we use the generic input functions instead of the nic
	  specific ones?

